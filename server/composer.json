{
  "require": {
    "twilio/sdk": "^5.16",
    "guzzlehttp/guzzle": "^6.3"
  },
  "require-dev": {
    "phpunit/phpunit": "6"
  },
  "scripts": {
    "start": "@php -S 0.0.0.0:8000",
    "test": [
      "@composer setupDB",
      "phpunit"
    ],
    "setupDB" : "@php dbSetup.php"
  }
}
